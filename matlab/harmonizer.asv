function [outputImg] = harmonizer(src,target)
%HARMONIZER Summary of this function goes here
%   Detailed explanation goes here
[rows, columns, numberofColorChannels] = size(target);
for x=1:rows
    for y=1:columns
        targe
    end
end
outputImg=src+target;
end

